<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MM4LB.Controls.Views.TemplateSlotsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:MM4LB.Markup"
    xmlns:help="using:MM4LB.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <GridView x:Name="Items" HorizontalAlignment="Center" Background="Transparent" Margin="-20 0 0 0" SelectionMode="None" IsItemClickEnabled="True" ItemContainerStyle="{StaticResource GameImagesGridViewItemStyle}" ItemClick="OnItemClick" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
        <GridView.ItemsPanel>
            <ItemsPanelTemplate>
                <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="3"/>
            </ItemsPanelTemplate>
        </GridView.ItemsPanel>
        <GridView.ItemTemplate>
            <DataTemplate>
                <Grid Width="333" ToolTipService.ToolTip="{Binding Name}" Padding="4">
                    <StackPanel>
                        <Border CornerRadius="8" Height="232" BorderThickness="1" BorderBrush="{ThemeResource CardBackgroundLightBrush}" Background="{ThemeResource BackgroundLightBrush}">
                            <Grid>
                                <Image Source="{Binding Image}" Stretch="Fill"/>
                            </Grid>
                        </Border>
                        <Border CornerRadius="0 0 8 8" Background="{ThemeResource AccentDarkBrushOpacity60}" Padding="0 4" Margin="0 -4 0 0">
                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" Foreground="{ThemeResource TextBrush}"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</UserControl>

<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MM4LB.Controls.Views.ImageTypeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MM4LB.Controls.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MM4LB.Models"
    xmlns:vm="using:MM4LB.Controls.ViewModels"
    xmlns:uc="using:MM4LB.Controls.Views" xmlns:template="using:MM4LB.Controls.Templates"
    xmlns:loc="using:MM4LB.Markup"
    xmlns:help="using:MM4LB.Helpers"
    mc:Ignorable="d">

    <!-- Una sola fila: combo (ancho fijo, la columna que lo aloja no cambia de anchura), filtros y favoritos (toman
         el resto en modo Full). El code-behind solo ajusta, según DisplayMode, la visibilidad de favoritos y el padding. -->
    <Grid x:Name="RootGrid" ColumnSpacing="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="228"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ComboBox Grid.Column="0" IsTextSearchEnabled="True" ItemsSource="{x:Bind ViewModel.SharedDataService.ImageTypesFiltered, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SharedDataService.SelectedImageSet, Mode=TwoWay}" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="models:PlatformImageSet">
                    <StackPanel Orientation="Horizontal">
                        <Border Background="{Binding ImagesCount, Converter={StaticResource ImageTypeCountToColorConverter}, Mode=OneWay}" CornerRadius="4" Width="40" Margin="0 2" Padding="0 0 0 2">
                            <TextBlock Text="{x:Bind ImagesCount, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                        </Border>
                        <TextBlock Text="{x:Bind Type.Value}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" Margin="8 2" Foreground="{ThemeResource SystemControlPageTextBaseHighBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <ToggleSplitButton Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" IsChecked="{x:Bind ViewModel.FiltersEnabled, Mode=TwoWay}">
            <SymbolIcon Symbol="Filter"/>
            <ToggleSplitButton.Flyout>
                <MenuFlyout>
                    <ToggleMenuFlyoutItem Text="{loc:Str Key=ImageType_WithImages_Label}" IsChecked="{x:Bind ViewModel.ActiveFilters.ImageTypes.HasImages, Mode=TwoWay}" Command="{x:Bind ViewModel.FiltersChangedCommand}"/>
                    <ToggleMenuFlyoutItem Text="{loc:Str Key=ImageType_WithoutImages_Label}" IsChecked="{x:Bind ViewModel.ActiveFilters.ImageTypes.MissingImages, Mode=TwoWay}" Command="{x:Bind ViewModel.FiltersChangedCommand}"/>
                    <ToggleMenuFlyoutItem Text="{loc:Str Key=ImageType_Favourites_Label}" IsChecked="{x:Bind ViewModel.ActiveFilters.ImageTypes.Favourites, Mode=TwoWay}" Command="{x:Bind ViewModel.FiltersChangedCommand}"/>
                </MenuFlyout>
            </ToggleSplitButton.Flyout>
        </ToggleSplitButton>

        <!-- Favoritos solo visibles en modo Full. -->
        <Grid x:Name="FavouritesHost" Grid.Column="2" Visibility="Collapsed" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <template:SeparatorEx VerticalAlignment="Stretch" HorizontalAlignment="Center" Thickness="2" Margin="16 -8" Orientation="Vertical" UseGradient="True" LineBrush="{ThemeResource AccentBrush}" />
            <TextBlock Grid.Column="1" Margin="0 0 8 0" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons" Text="&#xE735;" FontSize="20" Foreground="{ThemeResource AccentBrush}" Visibility="{x:Bind AnyFavouriteTypeButtonSelected, Mode=OneWay, Converter={StaticResource VisibleIfTrueConverter}}"/>
            <TextBlock Grid.Column="2" Margin="0 0 8 0" VerticalAlignment="Center" FontFamily="Segoe Fluent Icons" Text="&#xE735;" FontSize="20" Foreground="{ThemeResource TextSecondaryColor}" Visibility="{x:Bind AnyFavouriteTypeButtonSelected, Mode=OneWay, Converter={StaticResource HiddenIfTrueConverter}}" />

            <ItemsControl Grid.Column="2" ItemsSource="{x:Bind FavouriteImageTypeGroup.Items, Mode=OneWay}" HorizontalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="8" HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ToggleButton Width="120" Margin="0 0 4 0" IsChecked="{Binding IsSelected, Mode=TwoWay}" IsEnabled="{Binding IsEnabled, Mode=OneWay}" help:Help.Key="ImageType_Toggle_Tooltip">
                            <TextBlock Text="{Binding Value.Type.Value}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                        </ToggleButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>

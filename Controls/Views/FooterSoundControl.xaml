<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MM4LB.Controls.Views.FooterSoundControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MM4LB.Controls.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:help="using:MM4LB.Helpers"
    mc:Ignorable="d">

    <!-- Botón de sonido del footer: muestra el estado (altavoz con ondas / silenciado) y despliega un overlay con el
         slider de volumen y un botón de mute. El estado de sonido (silenciado o nivel 0) lo decide el ViewModel
         (IsSoundOff) y aquí solo alterna el glyph. -->
    <Button x:Name="SoundButton" Style="{StaticResource Button_Icon}" BorderThickness="0" Padding="10 0" VerticalAlignment="Stretch" help:Help.Key="FooterSound_Sound_Tooltip">
        <Grid>
            <FontIcon FontSize="18" Glyph="&#xE767;" Foreground="{ThemeResource AccentBrush}" Visibility="{x:Bind ViewModel.IsSoundOff, Mode=OneWay, Converter={StaticResource HiddenIfTrueConverter}}"/>
            <FontIcon FontSize="18" Glyph="&#xE74F;" Foreground="{ThemeResource TextSecondaryBrush}" Visibility="{x:Bind ViewModel.IsSoundOff, Mode=OneWay, Converter={StaticResource VisibleIfTrueConverter}}"/>
        </Grid>

        <Button.Flyout>
            <Flyout Placement="Top">
                <Flyout.FlyoutPresenterStyle>
                    <Style TargetType="FlyoutPresenter">
                        <Setter Property="Background" Value="{ThemeResource BackgroundBrushOpacity80}"/>
                        <Setter Property="BorderBrush" Value="{ThemeResource AccentDarkBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="CornerRadius" Value="8"/>
                        <Setter Property="Padding" Value="12"/>
                        <Setter Property="MinWidth" Value="0"/>
                        <Setter Property="MaxWidth" Value="320"/>
                    </Style>
                </Flyout.FlyoutPresenterStyle>

                <Grid Width="260" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Mute on/off (a la izquierda del slider): alterna el silencio conservando el nivel del slider. -->
                    <Button Grid.Column="0" Style="{StaticResource Button_Icon}" BorderThickness="0" Padding="6" VerticalAlignment="Center" Click="MuteButton_Click" help:Help.Key="FooterSound_Mute_Tooltip">
                        <Grid>
                            <FontIcon FontSize="18" Glyph="&#xE767;" Foreground="{ThemeResource AccentBrush}" Visibility="{x:Bind ViewModel.IsSoundOff, Mode=OneWay, Converter={StaticResource HiddenIfTrueConverter}}"/>
                            <FontIcon FontSize="18" Glyph="&#xE74F;" Foreground="{ThemeResource TextSecondaryBrush}" Visibility="{x:Bind ViewModel.IsSoundOff, Mode=OneWay, Converter={StaticResource VisibleIfTrueConverter}}"/>
                        </Grid>
                    </Button>

                    <Slider Grid.Column="1" Minimum="0" Maximum="100" StepFrequency="1" VerticalAlignment="Center" Value="{x:Bind ViewModel.VideoVolume, Mode=TwoWay}"/>
                </Grid>
            </Flyout>
        </Button.Flyout>
    </Button>
</UserControl>
